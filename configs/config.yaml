# Training configuration for Pattern Shape prediction model 
# (part I of NeuralTailor)

# 0.5 * stitch cls loss (focal loss) + 0.005 * stitch ce loss + 5 * panel loss
defaults: 
  - data_wrapper: garment_pcd_wrapper
  - model: pcd2garment
  - trainer: pcd_trainer
  - system: my_system
  - experiment: pcd_experiment

test_only: False
random_seed: 42
step_trained: null
pre_trained: null
storage_dir: null